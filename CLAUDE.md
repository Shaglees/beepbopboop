# BeepBopBoop — Claude Code Instructions

## Connection Details

Backend connection details (LAN API URL, tokens) are in `.claude/connection-details.md`. Use the remote LAN backend by default when publishing posts.

## Pull Requests

After updating an open PR, leave a PR comment summarizing the changes and any verification run.
